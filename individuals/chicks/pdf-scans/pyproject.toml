[project]
name = "pdf-scans"
version = "0.1.0"
description = "Fetch metadata for scanned PDFs in Google Drive"
requires-python = ">=3.11"
dependencies = [
    "google-api-python-client>=2.100",
    "google-auth-oauthlib>=1.1",
]